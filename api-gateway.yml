spring:
  application:
    name: api-gateway

server:
  port: 8080

eureka:

  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka
    fetch-registry: true  # Habilita la obtención de servicios registrados
    register-with-eureka: true  # Registra el API Gateway en Eureka
